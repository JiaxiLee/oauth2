<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.li.oauth.gateway.dao.MenuDao">
    <select id="findByUrl" resultType="com.li.oauth.gateway.entity.Menu">
        select * from u_menu where m_url = #{url}
    </select>
</mapper>